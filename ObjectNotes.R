# R Objects 

# Classes: Character, numeric (real numbers), integer, complex, logical(True/false)

#The most basic object is a vector 

#Vector can only contain objects of the same class, except lists 

#Empty vecots can be created with vector(type,length)

#1 numeric 1L integer

#Inf is infinity 

#NaN not a number 

#Objects have attributes like names, dimnames, dimensions, class, length
#attributes can be accesed by attributes()